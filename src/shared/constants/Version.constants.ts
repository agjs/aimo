/**
 * @file Version.constants.ts
 * @layer shared
 * @description Package version exposed to the CLI (release-please updates root package.json).
 */

/** Semantic version of the ai-model-orchestrator package (mirrors `package.json`). */
export const PACKAGE_VERSION = '0.0.0' as const;
