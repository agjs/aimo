# Plan — `aimo fetch` *(superseded)*

**This file is superseded** by the single merged backlog:

**[`plan-token-local-optimization.md`](./plan-token-local-optimization.md)**

The former standalone **`aimo fetch`** spec lives there as **§ Phase A — Web ingest (`aimo fetch`)**.

When executing, use the merged doc as the source of truth (principles, phases, execution gate).
